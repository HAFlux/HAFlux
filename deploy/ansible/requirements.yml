---
# Версии зафиксированы под ansible-core 2.16 (Ubuntu 24.04 / Debian 12 default).
collections:
  - name: community.docker
    version: ">=3.10.0,<4.0.0"
  - name: community.general
    version: ">=8.0.0,<10.0.0"
  - name: ansible.posix
    version: ">=1.5.4,<2.0.0"

---
- name: Deploy HAFlux control-plane (api + db + redis + haproxy)
  hosts: control_plane
  become: true
  roles:
    - common
    - sysctl_tuning
    - docker
    - { role: xanmod, when: haflux_install_xanmod | default(false) }
    - control_plane
